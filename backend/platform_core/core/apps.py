from django.apps import AppConfig


class CoreConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = "platform_core.core"   # Python path
    label = "platform_core_core"
