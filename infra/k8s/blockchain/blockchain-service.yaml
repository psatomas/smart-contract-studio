apiVersion: v1
kind: Service
metadata:
  name: blockchain-service
spec:
  selector:
    app: blockchain
  type: ClusterIP    # Change to LoadBalancer if you want external access
  ports:
    - protocol: TCP
      port: 8545
      targetPort: 8545