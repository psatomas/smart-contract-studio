services:
  anchor:
    build: .
    container_name: anchor_container
    volumes:
      - ./:/workspace
    ports:
      - "8899:8899" # RPC
      - "8900:8900" # WebSocket
      - "8901:8901" # Optional
    stdin_open: true
    tty: true